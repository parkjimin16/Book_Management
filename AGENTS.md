# book_management 프로젝트 지침

이 파일은 `book_management` 저장소에서 작업하는 Codex를 위한 프로젝트 맥락과 지속 지침이다. 저장소 루트에 두고 사용한다.

## 1. 프로젝트 목적

- C# WinForms로 운영되는 도서관리프로그램을 개발한다.
- 일반 사용자는 로그인과 회원가입을 할 수 있다.
- 관리자는 기본 관리 화면에서 도서와 회원 정보를 조회하고 관리한다. 신규 도서 데이터는 기존 화면과 스키마에서 정의된 방식으로 다룬다.
- 데이터는 MSSQL의 `LibraryDB`를 기준으로 한다.
- 기존 화면 흐름과 데이터 구조를 보존하면서 기능을 완성하고 안정성을 높이는 것이 우선이다.

## 2. 기술 스택

- 언어: C#
- 런타임/프레임워크: .NET 8
- UI: Windows Forms(WinForms)
- 데이터베이스: Microsoft SQL Server(MSSQL)
- 데이터베이스명: `LibraryDB`
- 데이터 접근: 현재 프로젝트에서 사용 중인 SQL 클라이언트와 접근 방식을 우선한다.

새 패키지, ORM, UI 프레임워크 또는 아키텍처를 임의로 도입하지 않는다. `Microsoft.Data.SqlClient`와 `System.Data.SqlClient` 중 무엇을 쓰는지는 프로젝트 파일과 기존 코드를 먼저 확인하고, 특별한 이유 없이 서로 바꾸지 않는다.

## 3. 현재 알려진 데이터 영역

현재 전달된 요구사항상 `LibraryDB`에는 다음 세 가지 업무 영역의 테이블이 있다. 아래 명칭이 실제 SQL 식별자라는 뜻은 아니다.

- 도서
- 회원
- 신규 도서

이 문맥에는 실제 물리 테이블명, 열 이름, 기본 키, 외래 키, 제약 조건이 기록되어 있지 않다. 따라서 `Books`, `Members`, `NewBooks` 같은 이름을 사실로 가정하지 말고, 작업 전에 다음 순서로 확인한다.

1. 기존 SQL 문자열, 데이터 접근 클래스, 모델 및 바인딩 코드를 검색한다.
2. 저장소에 스키마 또는 SQL 스크립트가 있으면 이를 확인한다.
3. 접근 가능한 개발 DB가 있다면 읽기 전용 메타데이터 조회로 실제 스키마를 확인한다.
4. 그래도 확인되지 않으면 임의의 이름으로 구현하지 말고 사용자에게 필요한 정보만 질문한다.

코드와 이 문서가 충돌하면 실제 코드와 DB 스키마를 근거로 차이를 보고하되, 요구사항을 조용히 변경하지 않는다.

## 4. 핵심 기능 요구사항

### 로그인

- 입력값을 검증한 뒤 `LibraryDB`의 회원 정보로 인증한다.
- 로그인 성공과 실패를 사용자에게 명확하게 알리고, 성공 시 권한과 기존 화면 흐름에 맞는 다음 화면으로 이동한다.
- 로그인 쿼리는 반드시 매개변수화한다.
- 비밀번호, 연결 문자열 등의 민감 정보는 화면 메시지나 로그에 노출하지 않는다.
- `LoginForm`의 `isBusy`는 중복 클릭과 동시 로그인 요청을 막는 재진입 방지 상태다. 이 동작을 제거하지 않는다.
- 로그인 시작 전에 이미 처리 중인지 확인하고, 처리 시작 시 `isBusy = true`, 모든 성공·실패 경로가 끝난 뒤 `finally`에서 `isBusy = false`가 되도록 유지한다.
- 처리 중에는 필요하면 로그인 버튼을 비활성화하고, 완료 시 반드시 원상 복구한다.

### 회원가입

- 필수 입력값을 검증한다.
- 신규 회원 등록 전에 아이디 중복 여부를 DB에서 확인한다.
- 비밀번호와 비밀번호 확인 값이 일치할 때만 등록한다.
- 중복 아이디, 비밀번호 불일치, 필수값 누락, DB 오류를 서로 구분해 이해하기 쉬운 메시지로 안내한다.
- 중복 확인과 실제 INSERT 사이의 경쟁 조건을 고려한다. 가능하면 아이디에 DB 고유 제약을 두고, INSERT 시 발생하는 중복 오류도 처리한다. 기존 스키마 변경이 필요하면 먼저 사용자에게 알린다.
- 비밀번호를 로그에 남기지 않는다. 기존 저장 방식이 해시 기반이면 그대로 안전하게 사용한다. 평문 저장이 발견되더라도 요구 범위를 넘어 스키마와 인증 방식을 몰래 바꾸지 말고 보안 문제와 변경 영향을 먼저 보고한다.

### 관리자 기본 화면

- 관리자가 도서 관리, 신규 도서 관리, 회원 관리 기능으로 이동할 수 있는 기본 진입점이다.
- 화면이 열릴 때 필요한 초기 데이터는 비동기로 불러오고 UI가 멈추지 않게 한다.
- 현재 선택된 메뉴와 작업 결과가 사용자에게 명확히 보이게 한다.
- 기존 폼 전환 방식, 사용자 권한 전달 방식, 컨트롤 이름과 레이아웃을 먼저 파악한 뒤 수정한다.
- 데이터 로드 실패 시 애플리케이션을 종료시키지 말고 오류를 안내하며 재시도 가능한 상태를 유지한다.

### 도서 관리

- 도서 목록을 조회하고, 검색 또는 필터링하고, 등록·수정·삭제할 수 있어야 한다.
- 도서 입력값의 필수 여부와 데이터 형식은 기존 DB 제약과 화면 요구사항을 따른다.
- 도서 카테고리는 아래 고정 목록을 사용한다. 표시 문자열을 임의로 바꾸거나 유사 항목을 추가하지 않는다.

  1. 전체
  2. 총류
  3. 철학
  4. 종교
  5. 사회과학
  6. 자연과학
  7. 기술과학
  8. 예술
  9. 언어
  10. 문학
  11. 역사

- `전체`는 조회 필터에서 모든 카테고리를 뜻한다. 실제 도서의 저장 카테고리 값으로 사용하지 않는다.
- 등록·수정·삭제가 성공하면 현재 목록을 다시 불러와 화면과 DB 상태를 일치시킨다.
- 신규 도서 테이블과 일반 도서 테이블의 관계나 동기화 규칙은 확인되지 않았다. 두 테이블에 동시에 쓰거나 한쪽에서 다른 쪽으로 자동 이동시키기 전에 기존 코드와 스키마를 확인한다.

### 신규 도서 영역

- 신규 도서 목록이나 별도 관리 화면이 기존 프로젝트에 있다면 그 조회 및 관리 흐름은 기존 UI와 DB 구조를 따른다.
- 별도 관리 UI가 확인되지 않으면 새로운 화면을 요구사항으로 간주해 임의로 만들지 않는다.
- 신규 도서가 별도 엔터티인지, 일반 도서의 상태/부분집합인지, 별도 전시 목록인지는 스키마와 코드를 통해 먼저 확인한다.
- 확인되지 않은 관계를 추정해 중복 INSERT, 연쇄 DELETE 또는 자동 동기화를 구현하지 않는다.

### 회원 관리

- 관리자는 회원 목록을 조회하고 대상 회원을 선택해 정보를 수정하거나 삭제할 수 있어야 한다.
- 수정 가능한 필드는 다음 세 가지뿐이다.

  - 연락처
  - 이름
  - 비밀번호

- 회원 아이디와 그 밖의 필드는 수정하지 않는다. 화면에서도 읽기 전용으로 유지한다.
- 비밀번호를 변경하는 화면에 비밀번호 확인 입력란이 있다면 두 값의 일치를 검사한다. 빈 비밀번호의 의미가 '변경하지 않음'인지 여부는 기존 구현을 확인한 뒤 일관되게 처리한다.
- 수정 성공 후 목록과 상세 표시를 다시 불러와 최신 값을 보여 준다.

### 삭제 동작

- 삭제는 현재 선택된 도서 또는 회원의 고유 키를 기준으로 수행한다. 신규 도서 삭제 기능이 기존 UI에 있다면 같은 원칙을 적용한다.
- 선택된 항목이 없거나 키가 유효하지 않으면 DELETE를 실행하지 않고 안내한다.
- 실행 전에 삭제 대상을 식별할 수 있는 확인 대화상자를 표시한다.
- DELETE 문은 반드시 매개변수화하고, 표시값이나 행 번호가 아닌 실제 기본 키를 조건으로 사용한다.
- 실행 결과의 영향받은 행 수를 확인한다. 0행이면 이미 삭제되었거나 대상이 바뀐 경우로 처리하고 성공으로 가장하지 않는다.
- 성공한 뒤 목록을 다시 조회하고 선택 상태를 안전하게 초기화한다.
- 외래 키 제약이나 연관 데이터 때문에 실패하면 임의로 연쇄 삭제하지 말고 원인을 안내한다.
- 소프트 삭제와 물리 삭제 중 어느 방식인지 기존 스키마와 코드를 따른다. 확인 없이 삭제 정책을 새로 만들지 않는다.

## 5. 현재까지 알려진 코드 맥락

### `LoginForm.isBusy`

- 로그인 처리의 중복 실행을 방지하는 플래그다.
- 예외가 나도 상태가 풀리도록 `try`/`finally`를 사용한다.
- 여러 이벤트에서 같은 로그인 메서드를 호출하더라도 실제 DB 요청은 한 번만 진행되어야 한다.

### 이벤트 연결

- WinForms 이벤트는 `control.Event += Handler;` 형태로 연결한다.
- 기존 `Designer.cs`, 생성자 또는 폼 로드 코드에서 이미 연결됐는지 먼저 확인한다.
- 같은 핸들러를 중복 등록하지 않는다. 중복 등록은 클릭 한 번에 INSERT, UPDATE 또는 DELETE가 여러 번 실행되는 원인이 된다.
- `=`로 기존 이벤트 구독을 덮어쓰려 하지 않는다.
- 디자이너가 관리하는 `InitializeComponent()` 호출 순서와 자동 생성 코드를 훼손하지 않는다.

### 비동기 DB 접근

- DB 연결 열기, 조회 및 변경 작업은 가능한 한 `async`/`await` 기반 API를 사용한다.
- `.Result`, `.Wait()` 또는 UI 스레드를 막는 동기 대기를 사용하지 않는다.
- 이벤트 핸들러를 제외한 비동기 메서드는 `Task` 또는 `Task<T>`를 반환하고 이름에 `Async` 접미사를 붙인다.
- 연결, 명령, 리더는 `using` 또는 `await using`으로 확실히 정리한다.
- 로딩 중 버튼 상태와 `isBusy` 같은 플래그는 성공, 유효성 실패, DB 예외 등 모든 경로에서 복구한다.
- 백그라운드 스레드에서 WinForms 컨트롤을 직접 수정하지 않는다. `await` 이후 UI 컨텍스트 또는 필요한 경우 `Invoke`를 사용한다.

## 6. 코딩 스타일과 주의사항

- 먼저 기존 저장소의 네이밍과 구조를 따르고, 새 코드에는 일반적인 C# 규칙을 적용한다.
- 타입, 폼, 컨트롤 클래스, 메서드 및 공개 멤버는 `PascalCase`를 사용한다.
- 지역 변수와 매개변수는 `camelCase`, private 필드는 기존 스타일이 없다면 `_camelCase`를 사용한다.
- 한 메서드에는 한 가지 책임을 두고, UI 이벤트 핸들러에 긴 SQL과 복잡한 비즈니스 로직을 몰아넣지 않는다.
- SQL 문자열 결합으로 사용자 입력을 삽입하지 않는다. 모든 입력은 올바른 SQL 타입의 매개변수로 전달한다.
- `SELECT *`보다 필요한 열을 명시하고, 열 이름과 바인딩 이름을 일관되게 유지한다.
- 예외를 빈 `catch`로 삼키지 않는다. 사용자 메시지와 진단 정보는 구분하고 민감 정보는 제거한다.
- 연결 문자열이나 비밀번호를 소스에 새로 하드코딩하지 않는다. 기존 설정 방식을 확인하고 따른다.
- WinForms 디자이너 파일은 꼭 필요한 경우에만 최소한으로 수정한다. 수동 변경 시 디자이너가 다시 열리고 빌드되는지 확인한다.
- 요구사항과 무관한 대규모 이름 변경, 폴더 이동, 전면 리팩터링은 하지 않는다.
- 주석은 코드만으로 드러나지 않는 이유나 제약을 설명할 때만 사용한다.
- 사용자에게 보이는 메시지와 기존 UI가 한국어라면 새 메시지도 한국어로 통일한다.

## 7. DB 변경 지침

- 스키마 변경 전에 현재 테이블, 열, 타입, NULL 허용 여부, 키와 제약 조건을 확인한다.
- 사용자의 명시적 요청 없이 테이블이나 열을 삭제·이름 변경하거나 데이터를 일괄 변경하지 않는다.
- 스키마 변경이 필요하면 다음을 먼저 제시한다.

  - 변경 이유
  - 영향받는 화면과 코드
  - 적용 SQL 또는 마이그레이션 스크립트
  - 기존 데이터 보존 및 되돌리기 방법

- 여러 테이블을 함께 변경해 일관성이 필요한 작업은 트랜잭션을 사용한다.
- 운영 데이터가 있을 수 있으므로 파괴적인 SQL을 자동 실행하지 않는다.
- DB 변경 스크립트가 저장소에 있다면 재실행 가능 여부와 적용 순서를 문서화한다.
- 코드의 SQL, 모델, 바인딩, 유효성 검사 및 문서를 스키마와 함께 갱신한다.

## 8. Codex 작업 원칙

1. 작업 시작 시 솔루션, 프로젝트 파일, 폼, 모델, 데이터 접근 코드, SQL 스크립트를 먼저 탐색한다.
2. 사용자의 요구사항과 실제 코드 사이의 차이를 구분하고, 확인되지 않은 사실을 추정하지 않는다.
3. 현재 동작을 보존하는 최소 범위의 변경부터 수행한다.
4. 작업 범위를 넘어서는 중요한 설계 결정이나 DB 변경은 영향과 선택지를 설명한 뒤 확인을 받는다.
5. 기존 사용자 변경 사항을 덮어쓰거나 되돌리지 않는다.
6. 코드 변경 후 적절한 솔루션 또는 프로젝트를 `dotnet build`로 검증한다.
7. 테스트 프로젝트가 있으면 관련 테스트를 실행한다. 테스트가 없다면 핵심 흐름의 수동 확인 항목을 정리한다.
8. DB가 없어 통합 검증을 못 했다면 성공했다고 추정하지 말고 검증하지 못한 부분을 명시한다.
9. 로그인 중복 실행, 이벤트 중복 구독, UI 멈춤, SQL 주입, 선택되지 않은 행 삭제를 회귀 위험으로 우선 점검한다.
10. 완료 보고에는 변경한 파일, 사용자에게 보이는 동작, 실행한 검증, 남은 제약 또는 가정을 간결하게 포함한다.

## 9. 기능별 완료 기준

- 프로젝트가 .NET 8 대상으로 빌드된다.
- 로그인 버튼을 빠르게 여러 번 눌러도 로그인 DB 요청이 중복 실행되지 않는다.
- 회원가입 시 아이디 중복과 비밀번호 확인 불일치가 차단된다.
- 관리자 기본 화면에서 요구된 도서와 회원 관리 흐름으로 이동할 수 있다. 기존 UI에 신규 도서 관리 흐름이 있다면 함께 정상 동작한다.
- 도서 조회에서 지정된 11개 카테고리 표시가 정확하며 `전체` 필터가 정상 동작한다.
- 회원 수정은 연락처, 이름, 비밀번호만 변경한다.
- 등록·수정·삭제 후 화면이 DB의 최신 상태와 일치한다.
- DB 호출 동안 UI가 멈추지 않고, 오류가 나도 버튼과 `isBusy` 상태가 복구된다.
- 모든 사용자 입력 기반 SQL이 매개변수화되어 있다.

## 10. 아직 확인이 필요한 항목

실제 저장소를 연 뒤 아래 항목을 확인하고, 필요하면 이 문서를 갱신한다.

- 솔루션과 프로젝트 파일의 정확한 이름 및 실행 프로젝트
- 세 테이블의 실제 이름, 열, 키, 관계와 제약 조건
- SQL 클라이언트 패키지 및 연결 문자열 관리 위치
- 사용자와 관리자 권한을 구분하는 열 및 로그인 후 화면 전환 규칙
- 신규 도서 테이블의 정확한 역할과 일반 도서 테이블과의 동기화 규칙
- 비밀번호 저장 및 검증 방식
- 검색 조건, 정렬, 페이지 처리 등 목록 화면의 세부 동작
- 삭제 정책(물리 삭제 또는 소프트 삭제)과 연관 데이터 처리 규칙
- 자동화 테스트의 유무 및 실행 방법
